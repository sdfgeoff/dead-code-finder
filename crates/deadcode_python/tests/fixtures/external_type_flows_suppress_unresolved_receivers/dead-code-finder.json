{
  "roots": [{ "path": "pkg", "module": "pkg" }],
  "entrypoints": ["pkg/main.py"],
  "rules": {
    "constructors": [
      {
        "match": "structlog.get_logger",
        "producesType": "structlog.BoundLogger"
      }
    ]
  }
}
