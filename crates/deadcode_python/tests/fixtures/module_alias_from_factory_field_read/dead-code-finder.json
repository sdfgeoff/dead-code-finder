{
  "roots": [{ "path": "pkg", "module": "pkg" }],
  "entrypoints": ["pkg/main.py"],
  "rules": {
    "constructors": [
      {
        "match": "pkg.config.get_config",
        "producesType": "pkg.config.Config"
      }
    ]
  }
}
